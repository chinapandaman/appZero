{
    "type": "object",
    "required": ["allow_duplicates", "allowed_fields"],
    "properties": {
        "allow_duplicates": {"type": "boolean"},
        "allowed_fields": {"type": "array", "items": {"type": "string"}}
    }
}
