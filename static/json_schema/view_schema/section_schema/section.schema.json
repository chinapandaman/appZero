{
    "type": "object",
    "required": ["section_name", "section_title", "allowed_operation", "api_name"],
    "properties": {
        "section_name": {"type": "string"},
        "section_title": {"type": "string"},
        "allowed_operation": {
            "type": "array",
            "items": {"type": "string", "enum": ["create", "read", "update", "delete"]}
        },
        "api_name": {"type": "string"}
    }
}
